<UserControl x:Class="DS4MapperTest.Views.ActionOutputListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:DS4MapperTest.Views"
             mc:Ignorable="d"
             d:DesignHeight="120"
             d:DesignWidth="420">
    <Grid Grid.IsSharedSizeScope="True">
        <ItemsControl ItemsSource="{Binding OutputItems}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <views:QuickBindControl Grid.Row="0"
                                                HorizontalAlignment="Stretch"/>
                        <ContentControl x:Name="InlineEditorHost"
                                        Grid.Row="1"
                                        Margin="0,6,0,0"
                                        Visibility="Collapsed"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
